import templateTeaser from './_dolphin-teaser-template';

const contactWrapper = document.createElement("div");


const appendContactForm = (element) => {
    document.body.appendChild(contactWrapper);

};

const closeContact = () => {

}

alert('HUHU');